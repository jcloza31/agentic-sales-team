import ComingSoon from "@/components/ComingSoon";

export default function AnalyticsPage() {
  return (
    <ComingSoon
      title="Analytics"
      description="Real numbers on your pipeline — pitches drafted, brands worked, calls booked — plus a per-agent breakdown of who's producing what."
    />
  );
}
